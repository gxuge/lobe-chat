import { type LobeHubProps } from '@lobehub/ui/brand';
import { memo } from 'react';

export const OrgBrand = memo<LobeHubProps>(function OrgBrand() {
  // Hidden: LobeHub branding
  return null;
});
